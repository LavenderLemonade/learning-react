import React from "react";

const Greet = () => <h1> Hey Sam!</h1> //this is the ES6 way of doing arrow functions

export default Greet //we use default so that we can change the name and stuff when we import it